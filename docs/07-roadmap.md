# 七、推进节奏

整体原则是**感知层验证先行，美术最后动工**。

## 7.0 当前状态：PC 侧基本完成，固件阻塞于硬件

**Phase 0 的核心问题已在真实数据上回答。** 硬件到手前能做的事基本做完，
剩下的都需要设备在手。

### 已完成

| 项 | 结论 | 证据 |
|---|---|---|
| **三态判别** | ✅ 成立 | 家 0% 移动 / 通勤 52% / 办公 5%，家与公司指纹相似度 **0.0000** |
| **感知层算法** | ✅ 四轮实测修正 | 见 [2.4.1 ~ 2.4.3](02-sensing.md#241-滑动窗口真实数据驱动的修正) |
| **18 个系统** | ✅ 全部已实现 | [08-systems.md](08-systems.md) |
| **素材管线** | ✅ 全通 | 151 只 + 彩色配色 + 中文名 + 点阵字库 = **144.9 KB** |
| **验收平台** | ✅ 九个面板，S1~S13 均可交互验收（音频能真的听） | [tools/inspector](../tools/inspector/) |
| **存档结构** | ✅ 实测 897 B | 双 buffer 1.75 KB，主槽损坏能回退 |

### 仍然阻塞

| 阻塞项 | 卡住了什么 | 怎么测 |
|---|---|---|
| **续航天数** | 扫描频率上限 → 遭遇频率、地点时间分辨率 | 实测电流曲线 |
| **RTC 走时精度** | 状态轴是「小时」级还是「天」级；日切判定 → S10 连续天数 | 深睡 12 小时对比时间 |
| **外置 32.768kHz 晶振是否存在** | 同上 | 拆机目视或实测漂移 |
| **NFC 芯片类型** | 能否用 NFC 对时（[2.5](02-sensing.md#25-无连接带来的时间问题)） | 手机 NFC Tools 读型号 |
| **口袋 RSSI 基线** | 阈值标定 —— 目前用的是电脑天线的数据 | 装兜里对比桌面 |

⏳ 标记的参数（遭遇频率、逃跑概率、闪光概率）都依赖前两项。

## 7.1 感知层：四轮实测修正的记录

这一节值得单独留着 —— **每一轮都是真实数据推翻了纸上设计**。

| 轮次 | 发现 | 修法 |
|---|---|---|
| ① | 静坐 20 分钟报 11 次状态转换、30% 误判移动 | 滑动窗口出现率（窗口 4） |
| ② | 窗口平滑把通勤的渐变也压低，沿路建 5 个地点 | 加 AP 新鲜度作第二判据 |
| ③ | 公司 27% 扫描只有 ≤4 个 AP，仍误判 34 次 | AP < 5 时不信距离 |
| ④ | 加了③仍有 15 次，且新鲜度全为 0 | 新鲜度为 0 时否决距离 |

最终三份真实数据全部达标（详见
[2.4.3](02-sensing.md#243-稀疏环境的两条防护公司数据暴露的)）。

第 ④ 轮的洞察值得记住：**「一个新 AP 都没见到」比「指纹变了多少」是更硬的证据** ——
真在移动一定会撞见没见过的 BSSID。这条比窗口平滑更本质，
加上它之后连窗口 1 都从 60% 误判降到 1%。

## 7.2 玩法系统：四处实测暴露的平衡缺陷

同样是跑起来才发现的（前三处详见
[S3-battle.md](systems/S3-battle.md)，第四处是真机 P1 上线后才暴露的）：

| 缺陷 | 后果 | 修法 |
|---|---|---|
| 野怪等级跟着主宠涨 | 主宠 Lv40 打 ★★ 照样输 —— **练级毫无意义** | 绝对等级带（★=5 … ★★★★★=45） |
| 种族值不随等级成长 | 等级只是个数字 | `effective_stat = base × (1+Lv/50)` |
| `capture_rate` 直接当像素 | 皮卡丘撞满 200px 上界，球种与养成加成全失效 | `BASE_SCALE = 0.55` |

修完后成长曲线才成立：小火龙→★、火恐龙→★★、喷火龙→★★★★，
而 ★★★★★ 连 Lv45 喷火龙也打不过 —— **传说级需要更多投入，这个留白是有意的**。

### 第四处：体能只涨不落（真机 P1 上线后发现）

S4 的体能只有两个消耗口：`play()` 的 −5、移动事件的 −2/次。
而 P1 待机页两个都不产生 —— 实测**静置 2 小时体能就顶到 100 并永远停在那**。

三条轴里体能因此不参与「消沉」判定（消沉看的是三轴最小值，
而体能常驻 100），等于把三轴设计退化成两轴。

这不是移植缺陷 —— `firmware/main/nurture.c` 与 `sim/gameplay.py` 逐拍对账
一致（`tools/pipeline/verify_nurture.py`），PC 侧本来就是这个行为。
之前没暴露是因为 PC 侧的 orchestrate 一直在喂移动事件。

三个候选修法，**都要先想清楚体能在玩法里到底代表什么**：

| 修法 | 含义 | 副作用 |
|---|---|---|
| 加一个基础衰减 | 体能 = 精神状态，不动也会倦 | 与「休息恢复」语义冲突 |
| 恢复上限压到饱食/心情之下 | 体能 = 由吃与心情供能 | 三轴变成两轴 + 一个派生量 |
| 战斗/遭遇消耗体能 | 体能 = 行动力，与 S1/S3 挂钩 | 要等 F9 接上遭遇才验证得了 |

倾向第三个（体能作为行动力最符合原版语义），但**它依赖 F9**，
所以现在只记录不改 —— 改了也没有遭遇来消耗它，验证不了。

## 7.3 Phase 0 剩余部分：硬件到手后的第一周

**只做感知层与硬件验证，不写玩法**（玩法逻辑已在 PC 上验证过）。

写一份最小固件，全程不联网、只做被动扫描：

- 每 30 秒记录 BSSID 集合（含 RSSI 排序）、SSID、OUI、时间戳
- 工作集放 RTC slow memory，仅在状态转换与每小时提交 flash
- 屏幕显示当前判定状态、今日转换次数、各态驻留时长

然后**揣在兜里过一周**，同时实测电流曲线与 RTC 漂移。

### 验证目标

| 待测项 | 判定标准 | 影响 |
|---|---|---|
| **续航天数** | 该占空比下能撑几天 | 决定扫描频率上限 |
| **RTC 走时误差** | 一周累积漂移多少 | 决定 S4 时间尺度、S10 日切判定 |
| **口袋 RSSI 基线** | 装兜里与放桌上的差值 | 重新标定 [2.4](02-sensing.md#24-匹配算法与两级扫描) 的权重曲线 |
| **两级扫描效果** | 0.3s 确认 + 1.4s 完整能省多少 | 验证自适应占空比 |

**PC 侧的算法可以直接搬** —— `sim/sensing.py` 刻意保持在
可用定点整数重写的范围内（无浮点必需、无动态分配、状态量定长）。

### 口袋标定的做法

同一地点、同一时段，各采 20 分钟：

```bash
tools/collector/collect.sh -i 30 -o data/raw/desk.ndjson     # 桌面基线
tools/collector/collect.sh -i 30 -o data/raw/pocket.ndjson   # 装兜里
```

对比两份的 RSSI 分布差值，用它修正 `RSSI_FLOOR` / `RSSI_CEIL`。
更稳的做法是**不依赖绝对值、只用相对排序**（见
[2.6](02-sensing.md#26-口袋标定一个会坑人的实测陷阱)）。

## 7.4 固件实现顺序

硬件到手后按这个顺序，每步都有可验证的产出：

| 步 | 内容 | 验证方式 |
|---|---|---|
| F1 | 点亮屏幕 + 三键输入 | 按键有反应 |
| F2 | 分块横带渲染 + 载入 sprite | 屏幕上出现皮卡丘 |
| F3 | 中文点阵渲染 | 显示「妙蛙种子」 |
| F4 | WiFi 被动扫描 + 落盘 | 串口 dump 出 NDJSON，格式与 PC 侧一致 |
| F5 | 移植 `sensing.py` → 定点整数 | 同一份数据，固件与 PC 算出同样的判定 |
| F6 | 深睡 + RTC + 唤醒 | 实测续航与漂移 |
| F7 | 存档双 buffer + CRC | 拔电测试不丢档 |
| F8 | P1 待机页 + S4 养成 | **最小可玩闭环** ✅ 截图核对 + 逐拍对账 |
| F9 | P2/P3/P4 + S1/S2/S3 | 完整收集循环 |
| F10 | P5/P6/P7 + S5/S9/S10 | 全功能 |
| F11 | S11 开场 + 首次启动引导 | 冷启动能选伙伴并存档 |
| F12 | S13 音频（PWM/I2S 二选一） | 能听见开场两声，实测功耗增量 |
| F13 | S18 自动存档接入各系统 | 拔电测试：捕获后立刻拔不丢 |
| F14 | S17 道馆（长线目标） | 八馆解锁条件按 biome 驻留触发 |

**F5 是关键的一致性检验**：把 `data/raw/*.ndjson` 灌进固件，
它应当算出与 `sim/replay.py` 完全相同的状态序列。不一致就说明移植有偏差。

**F8 是第一个「能玩」的里程碑**。它独立于感知层与捕获战斗，
且已经是完整体验 —— Tamagotchi 的空间输入是零，照样能撑几个月。

F8 已完成（提交 `3aaf05a`）。落地时多出一条没预料到的依赖：
**渲染类的东西必须能看到屏幕**。为此插了个 F10 截图通道
（固件把帧缓冲 base64 吐串口，`tools/device/screenshot.py` 存 PNG）——
它抓出的三个缺陷全部是「每一层自检都通过」的类型：
汉字渲成空心方框、整屏颜色错乱、字库缺 L/v 导致「Lv12」少两个字母。

F8 的第二课在验证方式：`nurture.c` 的自检七条全绿，
而与 `sim/gameplay.py` 逐拍对账（`verify_nurture.py`）立刻抓出
一个定点时间累加丢余数的缺陷 —— 真机 tick 间隔下每拍丢掉 41% 的时间。
**自检只能证明「符合我写它时的预期」，对账才能证明「与上游一致」。**

## 7.5 不阻塞硬件但尚未做的

**这一节已清空。** 三项（昵称存储、UI 文案定稿、音频）都已落地：

| 原待办 | 结论 |
|---|---|
| 主宠昵称的存储 | 原计划「存字库索引 4 字 = 8 B」。落地时先算取名代价：420 字的字库里 B 键逐字选，4 字昵称要按 840 次 —— **自由取名在三键设备上不成立**。改 24 个预设候选 + 1 字节索引，13 次按键。见 [S12](systems/S12-naming.md) |
| UI 文案定稿 | 建 [`sim/strings.py`](../sim/strings.py) 作单一来源（原本散在三处必然漂移），带按像素的排版审计。八个页面文档由 [`gen_pages.py`](../tools/pipeline/gen_pages.py) 生成，见 [pages/](pages/README.md) |
| 音频 | 四通道 APU 实现 + 11 条音效 = **164 B**（PCM 要 150 KB）。验收面板用 WebAudio 现场合成，**无硬件也能听**，JS 与 Python 逐条比对 11/11 一致。见 [S13](systems/S13-audio.md) |

### 音频仍需真机确认的部分

| 项 | 为什么现在测不了 |
|---|---|
| 音频通路是 PWM 还是 I2S | 规格表只写「扬声器」，没写驱动方式 |
| 是否有耦合电容 | 决定去直流是必需还是保险 |
| 实际音量与音色 | 小扬声器频响未知，低音可能整体听不见 |
| BGM 的续航代价 | 音效是事件驱动（几百 ms），BGM 是持续的 —— 功耗量级完全不同 |
