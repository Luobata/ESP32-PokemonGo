# P8 取名

> **本文档由 [`tools/pipeline/gen_pages.py`](../../tools/pipeline/gen_pages.py) 生成。**
> 文案与按键的单一来源是 [`sim/strings.py`](../../sim/strings.py) —— 改文案改那里，然后重新生成。

**进入路径**：P5 选「取名」按 A

**涉及系统**：S12 取名

## 三键

| 键 | 动作 |
|---|---|
| **A** | 确认 |
| **B** | 下个 |
| **C** | 上个 |

底部提示行：`[A]确认 [B]下个 [C]上个`　**184px / 232px**（余 48px）

## 文案

- `title` —— 「取名」（32px）
- `groups` —— 「叠字」、「属性」、「单字」、「称号」
- `unnamed` —— 「未命名」（48px）
- `keep` —— 「保持原名」（64px）

## 预设候选

共 24 个，分 4 组（来源：[`sim/naming.py`](../../sim/naming.py)）：

| 组 | 候选 |
|---|---|
| 叠字 | 小家伙、豆豆、果果、乖乖、团子、糯米、饭团、肉丸 |
| 属性 | 闪电、火苗、水滴、叶子、石头、毛球 |
| 单字 | 风、云、星、宝 |
| 称号 | 大王、队长、老大、阿宝、元气、伙计 |

最坏按键 **13 次**（双向循环；单向要 25 次）。

## 设计要点

### 为什么不做自由取名

算出来的结论，不是妥协：

```
字库 420 字，B 键循环切字 → 平均按 210 次选中 1 个字
取 4 字昵称 ≈ 840 次按键
```

而会话预算是 30 秒。就算把备选砍到 32 字也要 64 次按键。
**自由取名在三键无触摸设备上不成立。**

原版 GB 有十字键 + A/B + 完整假名表，那是另一种输入条件。

### 默认不占字节

`0xFF` = 没起过名 → 显示物种中文名。于是**不改名的玩家一个字节都不额外花**，
改过名的也只花 1 个字节（对比 UTF-8 12 字节）。

这也让「不改名」是个完全合理的选择，而不是显示一个占位符。

### C 键给「上一个」

24 个候选里转错一步就要再绕 23 次。这是纯线性列表，
**双向移动是这里最值钱的一个键** —— 最坏按键从 25 次降到 13 次。

### 分组是为了翻起来有方向感

四组（叠字/属性/单字/称号）在 B 键循环时相邻。玩家能感到
「这一片是叠字的、这一片是属性的」，比按拼音排更好用。
