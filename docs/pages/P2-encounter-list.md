# P2 遭遇列表

> **本文档由 [`tools/pipeline/gen_pages.py`](../../tools/pipeline/gen_pages.py) 生成。**
> 文案与按键的单一来源是 [`sim/strings.py`](../../sim/strings.py) —— 改文案改那里，然后重新生成。

**进入路径**：P1 按 C（有待处理时）

**涉及系统**：S1 遭遇累积、S8 闪光

## 三键

| 键 | 动作 |
|---|---|
| **A** | 选中 |
| **B** | 返回 |
| **C** | 丢弃 |

底部提示行：`[A]选中 [B]返回 [C]丢弃`　**184px / 232px**（余 48px）

## 文案

- `title` —— 「刚才路上遇到」（96px）
- `hint` —— 「稀有度越高越难捕获」（144px）
- `empty` —— 「队列是空的」（80px）
- `biomes` —— 「野外」、「住宅区」、「办公区」、「商业区」、「交通枢纽」
- `dropped` —— 「队列满了，丢掉最旧的」（160px）

## 设计要点

### 为什么要有列表这一层

遭遇是**后台累积**的（设备在兜里，屏幕不亮）。掏出来时可能攒了三五只，
直接进战斗就没得选。列表让玩家先扫一眼稀有度，决定处理顺序 ——
这是 30 秒会话里最值钱的 3 秒。

### C 键是「丢弃」而不是「返回」

返回给了 B。丢弃需要一个键，因为队列容量 16
（[S1](../systems/S1-encounter.md)），满了会自动丢**最旧的低稀有度**；
但玩家可能想主动清掉不想要的，腾位置给接下来的路程。

### 无动效

纯静态列表。这一页的作用是「看清」，动效只会干扰；且省电 ——
它可能是玩家停留最久的一页。
