# 状态与待办总览

> 生成于 2026-09-05，对应提交 `6fb0fb1`。
> 这份是**按系统的完整清点**，与 [00-handoff.md](00-handoff.md) 的
> 「重启会话入口」互补 —— 那份讲怎么接着干，这份讲还剩多少。

## 一句话

**核心闭环通了**：设备自己攒遭遇 → 看列表 → 打 → 抓 → 进图鉴 → 拔电不丢。
19 个系统里 **8 个已在真机跑通**，11 个还只在 PC 侧。

## 一、19 个系统的移植状态

| 系统 | PC 侧 | 固件 | 真机验证 | 备注 |
|---|:--:|:--:|:--:|---|
| S1 遭遇累积 | ✅ | ✅ | ✅ | 猎场+基地双路径，961 组对账 |
| S2 捕获判定 | ✅ | ✅ | ✅ | 四乘数窗口，791 组对账 |
| S3 自动战斗 | ✅ | ✅ | ✅ | 相克表 225 格逐值对账 |
| S4 养成 | ✅ | ✅ | ✅ | 3201 拍对账 + 52 分钟真机测速率 |
| S5 图鉴 | ✅ | ✅ | ✅ | 四张位图，剪影显示 |
| S6+S18 存档 | ✅ | ✅ | ✅ | NVS 292 B，拔电实测不丢 |
| S8 闪光 | ✅ | ✅ | ⚠️ | 判定通了，**没真见过一只**（1/512） |
| S15 转场 | ✅ | 🔸 | ✅ | 只接了抖动+闪白，8 种转场未接 |
| **S7 进化** | ✅ | ❌ | — | 固件只有数据字段，无逻辑 |
| **S9 道具** | ✅ | ❌ | — | 球数写死 `{12,3,1}` |
| **S10 成绩** | ✅ | ❌ | — | P7 页面也没做 |
| **S11+S16 开场** | ✅ | ❌ | — | 冷启动直接进 P1，没有选伙伴 |
| **S12 昵称** | ✅ | ❌ | — | P8 页面没做 |
| **S13 音频** | ✅ | ❌ | — | ES8311 已初始化，没发过声 |
| **S14 队伍仓库** | ✅ | ❌ | — | 主宠写死 #25 Lv12 |
| **S17 道馆** | ✅ | ❌ | — | 长线目标，依赖 biome |
| S19 编排 | ✅ | n/a | — | PC 侧概念，固件由 world.c 承担 |

## 二、八个页面

| 页 | 状态 | 缺什么 |
|---|:--:|---|
| P1 待机 | ✅ | 亲密度还是「亲」字，没换 ♥ 点阵 |
| P2 遭遇列表 | ✅ | biome 字段恒为 0（classify_biome 没移植） |
| P3 战斗 | ✅ | **野怪用 back sprite**（背对玩家），front 图集没解析 |
| P4 捕获 | ✅ | 球数写死；换球只切名字不切图（三种球共用 ball_24） |
| P5 照料 | ❌ | A 键现在直接喂食，没有独立页面 |
| P6 图鉴 | ✅ | A 键「详情」未实现 |
| P7 成绩 | ❌ | 依赖 S10 |
| P8 昵称 | ❌ | 依赖 S12 |

## 三、按优先级的待办

### P0 — 挡住「像个完整游戏」

**① 经验与升级没接**（新发现，与 handoff 的历史 P0-① 同源）

`battle_run` 算出 exp 并显示「经验 +N」，但**没人接收**：
`world.c:141` 是 `sv->exp = 0`，等级在三处写死 12
（`play_battle.c:80` 的 `PET_LEVEL`、`world.c:140`、`play_idle.c` 的 `s_pet`）。

后果是打赢没有任何长期回报 —— 而 S3 的整个难度梯度
（Lv12 打得过 ★1、打不过 ★3）建立在「等级会涨」上。
PC 侧的 `exp_for_level`/`exp_to_level` 已有且校准过，是接线工作。

**② 主宠是写死的 #25 Lv12**（S14）

抓到的怪进了图鉴位图，但**没有队伍** —— 抓了也不能用。
这让「收集」缺了下半场：收集的意义是换着打。

### P1 — 明显的体验缺口

**③ P3 野怪背对玩家**

front 是分尺寸档图集（40/56/72 三档，见 `convert_gen1.py`），
`assets.c` 只解了定长的 back。要加分段寻址。

**④ 开场流程缺失**（S11/S16）

冷启动直接进 P1 面对一只写死的皮卡丘。原版的「选伙伴」是
情感锚点，而 `sim/opening.py` 的大木博士台词已经写好了。

**⑤ 音频一声没有**（S13）

ES8311 codec 已初始化，`sim/audio.py` 的四通道 APU + 11 条音效
在验收平台上能听。捕获成功、升级、闪光遇到 —— 这几处最值。

### P2 — 已知但不急

- **⑥ 关机期间不衰减** —— 要墙钟时间，设备无 RTC 对时。
  `save_t.last_uptime_us` 已留位置，但只能知道「上次跑了多久」。
- **⑦ biome 恒为 0** —— `classify_biome` 没移植，P2 显示不出环境类型。
  连带 S17 道馆解锁条件也用不了。
- **⑧ 体能只涨不落** —— 见 [07-roadmap.md](07-roadmap.md#72) 第四处，
  三轴设计实际退化成两轴，修法依赖 S3 接上遭遇消耗。
- **⑨ 野外 biome 死代码** —— 需要真实户外采集，用户暂缓。
- **⑩ 闪光没真见过** —— 1/512，判定与概率都验过，但真机没撞上过。
  可以临时把分母调到 4 走一遍确认渲染。

### P3 — 长线

S17 道馆（八馆 + 四天王 + 赤红）、S10 成绩、S12 昵称、P5/P7/P8 三页。

## 三点五、下一步怎么开工（新会话照做）

按这个顺序。每一项都写了**改哪里、怎么验、别踩什么**。

### 第 1 步：接经验与升级（P0-①）

改动点：
- `firmware/main/world.h` 的 `world_t` 加 `uint32_t exp; uint8_t level;`
- `world.c` 加 `world_grant_exp(uint16_t amount)` —— 加经验、按
  `exp_to_level` 算等级、升级时打日志；**加锁**（与 world_feed 同款）
- `play_battle.c` 播完战斗时调它（现在只显示 `经验 +N` 没人收）
- 删掉三处写死的 12：`play_battle.c:80` 的 `PET_LEVEL`、
  `world.c:140` 的 `sv->level`、`play_idle.c` 的 `s_pet.level`
- `save_t` 已经有 `exp`/`level` 字段，只是一直写 0 —— 改成存真值，
  **`SAVE_VERSION` 要 +1**（字段语义变了，旧档该丢弃）

曲线在 PC 侧已校准：`sim/systems.py` 的 `exp_for_level(n) = 5n³/2`、
`exp_to_level(exp, cap)`。四个来源 `EXP_ON_CAPTURE/CARE/MOTION` = 60/30/8。

验证：
```bash
# 先加进 verify_battle.py：exp_for_level / exp_to_level 逐值对账
python3 tools/pipeline/verify_battle.py
# 真机：连打几场看等级涨
python3 tools/device/walk.py --keys e,c,A,b,a,a
```

**别踩**：`exp_to_level` 有个 `cap` 参数（徽章限制等级上限）。
现在没有徽章，传 `LEVEL_MAX` 即可 —— 但别把参数删掉，S17 要用。

### 第 2 步：S14 队伍与仓库（P0-②）

现在抓到的怪只进图鉴位图，**不能用**。收集缺了下半场。

改动点：
- 新增 `firmware/main/party.c/h`，移植 `sim/party.py`
- 队伍 6 只 + 仓库按物种 id 索引（**不是列表**，见那边的
  `box: dict` 与 `better()` 的取舍：同种只留 shiny > level > exp 的那只）
- `save_t` 加队伍区 —— sim 侧序列化是 1886 B，
  加上现有 332 B 约 2.2 KB，NVS 24 KB 装得下
- `play_capture.c` 捕获成功时除了 `dex_mark_caught` 还要 `party_receive`

验证：新增 `tools/pipeline/verify_party.py`，对账
「捕获 200 只后队伍与仓库的内容」—— sim 那边这条改过一轮
（列表→字典），有现成的期望值。

**别踩**：`receive()` 现在永不失败（仓库无上限，同种替换）。
早期版本会「仓库满了」拒收，实测浪费 225 次捕获。

### 第 3 步：P3 用 front sprite

野怪现在背对玩家。front 是**分尺寸档图集**（40/56/72 三档，
每档定长，段头记 `尺寸/单张字节/数量` 后跟 `(id, 位图)` 序列 ——
见 `tools/pipeline/convert_gen1.py` 的 `build_front_atlas`）。

`assets.c` 现在只解了定长的 back，要加分段寻址：
`species_t.flags` 的 bit2-3 存着尺寸档位。

验证：`walk.py` 截图看野怪是不是面朝玩家 + 尺寸变大。

### 第 4 步：S13 音频

ES8311 已初始化（启动日志有 `bsp_audio: ES8311 就绪`），一声没发过。
`sim/audio.py` 的四通道 APU + 11 条音效在验收平台上能听
（`tools/inspector/serve.sh` 有 WebAudio 现场合成）。

最值的三处：捕获成功、升级、闪光遇到。

**别踩**：音频要占 I2S DMA，而 WiFi 扫描也吃内存 ——
先测一次并发（扫描时放音效），别等接完才发现打架。

## 四、工程质量现状

**对账脚本**（`tools/pipeline/verify_*.py`，主机跑，不用烧板子）

    verify_battle     791 组   S2·S3 确定性逐值 + 三组难度梯度
    verify_encounter  961 组   S1·S5·S8，含队列全量内容比对
    verify_nurture   3201 拍   S4
    verify_sensing   ⚠️ 67 处不符（F5 历史残留，影响判定 2%，非回归）
    inventory_assets  资产断言，非零退出码可当 CI 门禁

**真机自助验证**

    tools/device/walk.py      走链路 + 逐页截图（dbg.c 从串口注入按键）
    tools/device/screenshot.py 单张截图
    tools/device/decay.py     衰减速率实测

截图存档在 [screens/](screens/)。

**预算**：flash 1.33 MB / 3 MB，资产 159 KB，DRAM 横带 37.5 KB。

## 五、这一轮踩过的坑（都已加守护）

| 缺陷 | 为什么没早发现 | 守护 |
|---|---|---|
| 推屏 DMA 竞争（紫屏闪烁） | 截图取交换前的数据，**绕开了出错环节** | 截图改取交换后 |
| 跨页面用队列下标（打A抓到B） | 每次验证都手动补救过，没连跑 | 自检 uid 稳定性用例 |
| 字库缺「对」 | 中文硬写在 C 里，管线看不见 | 扫源码 + 构建时自动同步 |
| sprite 内部高光漏底 | 151 只全中，但以为是素材如此 | 转换时洪泛填充 |
| 存档写成功却读不到 | NVS 初始化藏在 wifi_bring_up 里 | 提成 save_init() |

共同点：**观测覆盖不到的地方**，不是逻辑写错。
