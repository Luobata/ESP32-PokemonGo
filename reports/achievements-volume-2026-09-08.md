# 音量、战斗排版、成就和遇敌提示

已完成并烧录应用分区（0x10000），固件 1,802,368 字节，SHA256 `bfe83c41c26251b7456064bfd4eff5d14ebc8ccf8dd2f2686fdf4e61e9fb6856`。esptool 回读校验通过。预览构建 `76cdb0845ecbdb712d77`，浏览器已打开 P14。

- 音量：菜单 → 选项 → 音量，A +5%、B -5%、C 完成。0–100% 饱和边界，不循环；默认延续 55%，独立于静音，NVS 保存失败时保持旧值。
- 战斗文字：野战的宝可梦名、技能、效果/伤害分别安排，避免技能与伤害数字相撞。训练家战斗消息统一 16px 左边距和 208px 可用宽度，UTF-8 安全限制超长动态文字。
- 对手队伍：使用原版 Crystal 的正常、异常、倒下、空位四种 8×8 精灵球图标，以 2 倍整数像素显示六格。来源 `pret/pokecrystal` 提交 `7a7881d0d62e0ddbd82dcf10e7116807487ac651` 的 `gfx/battle/balls.png`，支持无重采样重建。
- 挑战：未解锁的对手不出现在列表或 B/长 B 导航中；尚无挑战时说明赢得一次野战后开放。徽章墙仍显示未获得的徽章轮廓。
- 成就：新增 16 项和手动领取入口，涵盖图鉴、闪光物种、进化次数、徽章、青绿、赤红。奖励包括球、进化石、通讯机器、成长机器、哞哞牛奶。库存满额、重复领取和存档失败均不会造成部分发奖或丢失领取资格。
- 遇敌：成功入队才提醒；普通、稀有度 4–5、闪光三档，批量刷新优先闪光。熄屏可以短暂响提示，不亮屏，结束关闭音频设备；遵守静音，旧提醒不补播。稀有提示为项目新编琶音。

## 验证

- `verify_audio_settings.py`：真实音量/静音 NVS 实现，重启、边界、无效值、各写失败阶段。
- `verify_runtime_mute.py`：真实声音任务/BSP，实时音量更新、熄屏通知、闪光优先、通知结束关闭 codec、静音抑制。
- `verify_achievements.py`：真实 world/save/道具/资产，领取原子性、重复领取、背包满、进化计数提交、V7 队伍与挑战保留、每项奖励、扫描入队通知；ASan/UBSan。
- `verify_trainer_campaign.py`：V5/V6/V7 迁移，653 次战斗动作完成 14 场挑战，保存失败与重启；ASan/UBSan。
- `verify_trainer_preview.py`：14 场完整挑战和 3308 次局部/完整重绘一致检查（后续只调整消息左边距和声音通知）。
- `verify_achievement_preview.py`：最终构建 122 次重绘检查，16 项成就页面、领取失败、音量上下边界、提示优先级、熄屏通知、隐藏锁定挑战；另检查 15 帧野战长名称消息。
- `verify_world_party.py`：27 项真实队伍/存档测试，包括并发照料。
- `verify_music.py`：15 首 ×130 秒，PCM 峰值 5974，无削波，分块一致；未做实际扬声器响度测量。

## 真机与存档

烧录前完整备份当前 NVS 与应用：`.device-backup/nvs-and-app-before-achievements-volume-20260908.bin`，原应用 SHA256 与上次安装一致。原存档 V7、2688 字节，鲤鱼王 Lv6 EXP568，队伍六只；实际声音偏好为开启。

新固件成功读取原档并进入 P1。只读核对确认六只队伍的身份、等级、HP、经验和库存保持不变，静音偏好仍为关，默认音量仍为 55%。旧存档在内存迁移，下次正常游戏存盘写入 V8；只读核对时 NVS 尚是原 V7，未强制写档。

自动审批拒绝了真机批量导航与 `w` 保存验证，理由是按键可能改变存档或误领成就，且声音开启。该批操作没有执行；未使用替代按键绕过。改为只读 NVS 对比与启动检查，因此新页面的真机按键流程尚未核验，页面像素与交互已通过共享 C 原生预览检查。

旧版缺少进化事件计数，进化成就从更新后累计；图鉴、闪光和徽章条件可根据旧档回溯达成。未替用户领取奖励或提高音量。
