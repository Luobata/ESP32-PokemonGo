# 仓库查找优化

按用户确认实现全部／闪光／可进化筛选、属性筛选、图鉴编号／等级排序和整屏翻页，不增加页码。长按 A 从仓库列表打开选项；A/B/C 和长按 B 的基本职责不变，长按 C 熄屏继续保留。

可进化复用自然进化等级判定和道具适用规则，并检查背包数量；伊布分支、通讯机器等与实际游戏一致。筛选不消耗道具。展示索引与真实仓库位置分离，排序或筛选不会改变存档，也不会把结果列表序号误当作要交换的个体。

验证通过：

- 满仓 151 位置、0／1／5／6／151 条结果的整屏循环边界。
- 图鉴编号排序、等级排序、原选中个体保留、同种个体位置稳定。
- 闪光与属性叠加、第二属性匹配、空结果清除筛选。
- 等级与道具进化条件、查询不消耗道具。
- 过滤后换入闪光个体、保存失败重试、换出的伙伴完整进入原仓库空位。
- 既有秘境结束确认的 11 项 UI 检查，实际 world/save/dungeon 的 24 个 ASan/UBSan 用例。
- 全屏与分条渲染一致、字库覆盖、选项和列表截图检查、ESP-IDF 构建、git diff --check。

QualityGate 本地返回 pass、无阻断，但没有脚本规则匹配 C/Python 路径；规则 MCP 和中央语义审查不可用，Mutation 保持禁用。本次有效验证以上述项目测试和编译为准。

新版仓库已按用户要求烧录，仅更新应用分区；固件写入校验通过，启动前 NVS 回读与备份逐字节一致。设备读入原存档并进入 P1 待机，12 秒启动观测无崩溃。当前固件与网页预览共用新增的 box_view 和 play_party 代码。

验收截图及回执：evidence/box-browser/。
