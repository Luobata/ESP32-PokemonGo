# P11 立即熄屏入口

选项页新增“立即熄屏”，与“译名”“返回菜单”组成三项。A 调用 `screen_idle_request_off()`，不切页、不改光标；B/长 B 正反循环，C 返回菜单。保留白底、原品像素边框与 cursor，三行位于 y80/112/144。

选中熄屏时显示“30秒无操作自动熄屏”“任意键亮屏”“按A立即熄屏”。超时秒数读取 `screen_idle_timeout_ms()`。首次按键只唤醒，避免在熄屏选项上按 A 又立即关屏。相关说明已同步 `docs/pages/P11-menu.md`。

`python3 tools/pipeline/verify_menu_screen_idle.py` 在实际原生构建 `c848fde23880291a4a86` 通过：三行/三条提示共 6 次字形像素比较、三项正反循环、手动熄屏全黑输出、A 唤醒不再触发页面动作、亮屏恢复同一画面、译名切换和返回正常；20 次横带/全重绘比较均为 0。新增菜单/队伍文案字形存在且非空。已查看真实 C 输出，文字未碰边框。

- [实际选项截图](evidence/screen-idle-2026-09-08/P11-screen-off-option.png)
- [菜单验证结果](evidence/screen-idle-2026-09-08/menu.json)

本次仅改菜单页面、页面说明与专项验证；屏幕服务、输入拦截、宿主接线由根任务实现。这里验证的是实际 C 菜单和宿主背光输出，未代替电源按钮硬件调查。
