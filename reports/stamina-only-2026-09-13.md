# 统一为体能消耗

取消探索情报24/24、进度条和额外4点储备。成功探索只扣5点全局体能；切磋5点，道馆/重赛/赤红10点，联盟整轮20点，沿用原费用。体能100上限、50点/小时不变。主页显示预计回满分钟数，探索页显示当前体能。

原情报加成合并进基础探索收益：旧余额不再影响稀有度和线索道具（基础60%）。移动只自动记录新地点并增加心情，地点发现仍用于探索加成；驻留不会产生可兑换资源。剧情线索是任务进度，保留。

沿用V14存档：energy保留原值但不消费、不补充、不参与收益；旧hunt_q10在有效扫描时清理且不兑换。失败保存不提交清理或地点进度，避免存档与运行状态不一致。设备当前固件尚未更新，未自动发布社区或推送Git。

验证：旧余额0/24各10000种子稀有与道具结果一致（四星及以上1069次、道具5954次）；38组路线/迁移/保存事务检查与20000步覆盖151种；110组体能事务回归通过。真实页面输入验证旧余额不影响像素、探索扣5体能，局部刷新与全量一致。ESP-IDF构建通过。页面证据见evidence/stamina-only-2026-09-13。
