已修复并烧录一处可导致绿色横幅的显示所有权冲突。

游戏通过 screen_push_band 直接写屏；旧 screen_own_display 仍让 LVGL 的绿色空屏在首次游戏画面后执行一次延迟刷新。待机页常规动画只重画第 0、1、3 横带，第 2 横带（y=160–239）仅在养成数据变化时重画，因此覆盖可留在屏幕中部。烧录／重启会重新触发此初始化顺序，不是由技能数据或存档损坏造成的这条代码路径。

修复：首次画游戏前，在 LVGL 锁内关闭 display invalidation 并暂停其 refresh timer。游戏定时器、按键、自动熄屏和 SPI 完成回调保留。空屏背景也改为白色。

验证：实际 vendored LVGL + 生产 screen.c，旧逻辑 16 次延迟刷新，修复后 0 次，业务 timer 正常回调 60 次；移除保护的负向对照重新检测到覆盖。ESP32 编译和应用写入哈希校验通过。启动日志明确显示保护已启用，原鲤鱼王 Lv6、队伍 6 只正常读回，声音偏好仍为 muted=0。

固件：1819072 字节，SHA256 21d4cbf925b3bfc37857350cf000c39a26b90306d7d4973946d59372a8163b77。只写应用分区 0x10000。之前的技能版本及 V9 存档已备份至 .device-backup/nvs-and-app-before-display-owner-20260908.bin。

尚未得到用户对具体页面、设备／网页及出现时机的补充，也没有物理屏幕照片；因此证明的是该覆盖路径已被消除，用户所见横幅是否完全消失仍待实机目视确认。串口截图会主动重画页面，不能作为排除后续 LVGL 覆盖的证据。
