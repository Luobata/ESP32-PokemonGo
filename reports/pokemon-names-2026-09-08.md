# 可切换宝可梦译名

已加入官方中文名与“金银民间旧译（资料版）”。公开金银表中 #149 快龙为“肥大”，水晶列则为“富阿”。本次只采用金银资料，来源覆盖、异名和未作 ROM 字节定版的边界见 [字典说明](../data/pokemon_names/README.md)。

`assets_species()` 是共享出口，所有固件页面读到同一选项。生成的 151 条名称只覆盖显示字段，原资产和物种编号保持原样；缺失条目回退官方名。

P5 照料页长按 A 双向切换：当前状态在 `(12,244)`；“长按A切换译名”在 `y262` 居中；进化提示仍在同一行右缘 228，不和状态相碰。硬件重启恢复官方。预览选择器即时更新当前画面，localStorage 保存已确认偏好；新场景与页面载入会显式传入偏好。硬件长按 A 的响应也会反向同步网页选择器。

验证通过：

- `verify_pokemon_names.py`：151 条官方名与资产原字节对照、151 条旧译与资料记录对照、全部非名称字段一致；私有构建删除 #149 后实际出口回退“快龙”。共 604 次实际资产读取，ASan/UBSan 通过。
- 142 个译名与新提示相关汉字都有非空 16px 字形。
- P0–P6、P9 八页切换后重绘差异为 0，切回官方后像素逐字节还原，游戏状态没有变化。P1/P2/P3/P4/P5/P9 可见名称更新；P0 和仅显示编号的 P6 图鉴网格保持像素一致。
- `verify_firmware_clock.js`：12 项输入、时钟与偏好用例通过，原有 5 个负例仍能抓住回归。在途旧帧不会覆盖新选择，长按 A 会同步偏好，新场景及重新打开会保留已确认选项，禁用 localStorage 时正常使用官方默认。
- 实际本地 HTTP 服务验证选择器命令、P5 长按 A 响应与 P9 新场景参数；4 种非法设置返回 400，既有会话仍可继续。见 [HTTP 结果](evidence/pokemon-names-2026-09-08/http.json)。

本次名称门禁构建：`4e927afa07140fc618d7`。完整数字与图片见 [验证结果](evidence/pokemon-names-2026-09-08/verification.json)、[照料旧译](evidence/pokemon-names-2026-09-08/P5-gs-legacy.png)、[对战旧译](evidence/pokemon-names-2026-09-08/P3-gs-legacy.png)。网页显示实际固件像素；历史 JS 设计稿仍不承担译名设置或固件同构验证。
